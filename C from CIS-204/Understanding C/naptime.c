#include <stdio.h>
#include <stdbool.h>
#include <string.h>

int main ()

    int input;
    char strInput[4];

    bool sleepy
    bool nearBed
    bool haveClass

    int sleepDebt
    int lastSleep

    printf("Welcome to Mr. Magic man's "Should you sleep" program.\n")
    printf("Are you currently sleepy? [Yes/No] \n");
    scanf("%s", strInput);

    if(strcmp(strInput, "Yes") == 0) {
        sleepy = true
    }
    else if(strcmp(strInput, "No") == 0) {
        sleepy = false
    }
        else {
            return 1;
    }
    
    printf("Are you currently near a bed?\n");

    printf("Do you have an upcoming class or a class at the moment?\n");

    printf("How much sleep debt do you have (in minutes)?\n");

    printf ("How much sleep did you get the last time you slept (in minutes)?\n");
    
    bool shouldSleep = 

    sleepDebt >= 60 || lastSleep  <= 450

    return 0;
