#include <stdio.h>
#include <stdbool.h>
#include <string.h>

int main(){

char example[] = JonJonJoseph;
char target[] = Jon;

int exampleTest[] = strlen(example);
int targetTest[] = strlen(target);

bool found = false;
bool match = false;



return 0;
}