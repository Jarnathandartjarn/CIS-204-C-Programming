#include <stdio.h>

int main(){

        char pattern[] = "cat";
        char target[] = "application";

        for(int i = 0; i < 11; i++) {

            int flp = pattern[i];
            int flt = target[i];
            
            for (int j = i; j < pattern[i]; ++j) {
                
            }
            }

     return 0;
    }